# 倉庫棧板判讀規則

這份文件記錄「倉庫全集」目前採用的棧板判讀方式，供後續接手與調整程式時參考。
最後更新：2026-08-24 V170

## 1. 分頁結構

- 每個分頁第一列（Row 0）是欄位標頭，格式為 `位置標頭 | 數量` 交替，如 `A-1 | 數量 | A-2 | 數量 | ...`
- A 欄（Col 0）是排標題與結構欄，非資料來源
- 資料從 Row 1 開始，每兩欄為一組：**Col 1 = 產品名稱 / Col 2 = 數量**，依此類推

## 2. 三軸座標系

- **行（Slot）**：A1 欄的標頭（如 `A-1`、`B-3`）
- **排（Depth）**：由上而下，`第一排` ～ `第十排`，每排以「第N排」標題列分隔
- **層（Level）**：由前而後，同排同色段內由上往下累計

## 3. 背景色與棧板邊界

- **深藍色 `#1A3A6A`**：分隔行，不含資料，直接跳過
- **紅色 / 黃色（散箱/混板）**：同色連續列視為同一物理棧板，可含多組編號/批號
- **綠色（專案庫存）**：同色連續列視為同一物理棧板
- **白色（正常庫存）**：每一列各自為獨立棧板（一列 = 一板），不與其他列合併
- 紅與黃視覺交替，但意義相同，邊界完全看背景色是否相同

## 4. 編號（SKU）與批號（Batch）的判斷

**不再依賴列位置（偶數/奇數）**，改用品號識別邏輯：

### 白底列（白色背景）
- 當某一列的產品欄值確認為「編號」時，系統會自動檢查下一列：若下一列為有效資料行且非編號（如 `GD6`, `GC4`, `/03`），則自動將下一列作為該編號的「批號」進行配對（消耗 2 列）。
- 若下一列為另一個編號、空白列或排標題，則該編號獨立為一板，批號預設為 `無批號`（消耗 1 列）。

### 彩色段（紅/黃/綠）
同一色段內逐列判斷，判斷流程：

```
該列值（prodCol）
   │
   ├─ 是 SKU Set 裡的已知編號？  ─→ 是編號（pending），等待批號
   ├─ 符合格式 /^[A-Z]{2}\d{4,}/？→ 是編號（pending），等待批號
   └─ 以上皆非                   → 視為批號，與前一 pending 編號配對
```

- **SKU Set**：來自四家公司產品目錄（高雅瓷、安帝嘉、喜悅納、漢樺）的所有「編號」欄位，大小寫不敏感（比對時統一轉大寫）
- **格式 fallback**：2 個大寫字母 + 至少 4 位數字（如 `RP12004`、`SL10011`、`LV12001`），排除批號格式 `TA4`、`GC4`、`AA6`（僅 1-2 位數字）
- **中文備註行**（如「不給出貨只能出樣」）：不含任何英數字元，直接跳過不處理

## 5. 發生順序

1. 進入新色段 → `groupSeqByDepth[currentDepth]++`，分配 `GroupId`
2. 該色段內逐列：
   - 若是 **編號**：存入 `pendingSkuRow`
   - 若是 **批號**：與 `pendingSkuRow` 配對，發出一筆 pallet（`_emitPallet_`），清除 pending
   - 色段結束時若有未配對的 pending（只有編號無批號）：發出 SKU-only pallet

## 6. 輸出欄位

| 欄位 | 說明 |
|------|------|
| `Sheet` | 分頁名稱（如 `B-G區`） |
| `Slot` | 行位置（如 `A-1`） |
| `Stack` | 排標題文字（如 `第一排`） |
| `Depth` | 排數字（1, 2, 3...） |
| `DepthLabel` | 排中文標籤 |
| `Level` | 層（同色段內累計） |
| `SKU` | 編號（產品代碼） |
| `Batch` | 批號，無則為 `無批號` |
| `BoxQty` | 編號行右側的箱數 |
| `PieceQty` | 批號行右側的片數 |
| `BoxQtyFontColor` / `PieceQtyFontColor` | 數量欄位字色 |
| `BgColor` | 背景色分類 |
| `FontColor` | 編號欄位字色 |
| `Status` | `正常庫存` / `混板/散板` / `專案庫存` |
| `IsLastPallet` | 是否為最末端棧板（藍字標記） |
| `PalletGroupId` | 同物理棧板分組 ID |
| `PalletKey` | 唯一識別鍵（Sheet\|\|Slot\|\|Depth\|\|Level\|\|SKU\|\|Batch） |

## 7. 總結

- 編號/批號判斷：**產品目錄查表 + 格式 fallback**，不再靠列位置
- 白底：每列獨立一板
- 彩色底：同色連續段 = 1 物理棧板，內含多組編號/批號配對
- 分隔線：`#1A3A6A` 深藍色
- 不使用 Bold 做邊界判斷
