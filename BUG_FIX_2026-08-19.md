# 🐛 2026-08-19 假庫位表 Codebase Bug Scan & Fix Record

## 背景

對 Node.js + Express 後端進行全面 code review，逐行對照 GAS 原版邏輯（`WarehouseParse.gs`、`WarehouseWriteback.gs`），交叉比對假庫位表（`1QR6xLZrdSUzhkCNwBhE5EUYpdv8GqkESfj3ELzNf59s`）的實際結構，識別出 12 個 bug 並逐一修復。

---

## ✅ Bug 1–3：`server/utils/colorUtils.js`（Critical）

### 問題
1. **RED 偵測使用 hex 精確比對**，無法辨識 Google Sheets API 實際回傳的粉紅色（`#ff6666`、`#e06666`、`#ff9999`），導致這些顏色被當作 `COLORED` 而非 `RED`，棧板邊界偵測錯誤。
2. **`WHITE_HEX` 包含 `'000000'`（黑色）**，黑底格子會被誤判為 WHITE。
3. **`identifyFontColor` 只分 `BLACK` / `COLORED`**，GAS 版能辨識 `BLUE` 字體色，前端靠此判斷「最後一板」狀態。

### 修法
完全重寫為 RGB ratio 演算法，對齊 GAS `WarehouseParse.gs:1-24` 邏輯：
- `identifyBgColor()`：先判 YELLOW（R>180 && G>180 && B<160），再判 RED（R>120 && R>G×1.2 && R>B×1.2 && G<R×0.65），再判 GREEN、PURPLE，其餘歸 WHITE。
- `identifyFontColor()`：判 BLUE（B>150 && B>R×1.3 && B>G×1.3）、RED、GREEN，其餘歸 BLACK。
- 移除 `WHITE_HEX` 精確比對清單，改用 GAS 的字符串比對 + RGB 解析。
- `FG_HEX` 新增 `BLUE: '#0000ff'`、`RED: '#ff0000'`、`GREEN: '#00b050'`。

---

## ✅ Bug 4–5：`server/services/depthSection.js`（Critical）

### 問題
1. **彩色段中的白色行**（混合格式）被當作獨立白色棧板處理，但未檢查 bold，且 `PalletGroupId` 用 `wht_${r}` 不穩定。
2. **`readDepthSection` 不回傳 `BoxQtyFontColor` / `PieceQtyFontColor`**，前端 `qtyStatusHtml()` 需要這兩個欄位顯示「已回報/待重點/無庫存」狀態。
3. **彩色棧板 bold 預掃範圍不對**：原版只掃 bold 出現的第一筆就 break，但 GAS 版預掃整個排段（`sectionHasBold`），確保同一排段所有列都使用一致的判斷邏輯。

### 修法
- 對齊 GAS `WarehouseWriteback.gs:readDepthSection_()` 的預掃邏輯：在遇到「排標題列」時，先對整個排段掃一次有無 bold 彩色格，記錄 `sectionHasBold`。
- 彩色行回傳 `BoxQtyFontColor`、`PieceQtyFontColor`（分別取 qtyCol 的 `fontColor`）。
- 白色行也回傳 `BoxQtyFontColor`（取 qtyCol fontColor）、`PieceQtyFontColor: 'BLACK'`。
- `detectFormat()` 增加排標題列跳過邏輯。

---

## ✅ Bug 6：`server/services/moveService.js`（High）

### 問題
`movePallet()` 只用 `sku` 做 `findIndex`，若同排有兩個相同 SKU 的棧板（不同批號/顏色），會永遠抓到第一個，導致使用者想搬 A 棧板卻搬了 B。

### 修法
`movePallet()` 新增 `batch` 參數，匹配邏輯改為：
1. 優先 SKU + Batch 精確匹配
2. 若 batch 為空（白色格式無批號），fallback 到 SKU-only match
3. 對齊 GAS `WarehouseWriteback.gs:1140-1156` 的候選 index + Level 比對邏輯

---

## ✅ Bug 7：`server/utils/gridUtils.js`（Verified OK）

經逐行對照 GAS `WarehouseWriteback.gs:findDepthRowRange_()`，Node.js 版本的 `startRow = r + 1` 與 `startRow = r + 1`（GAS）完全一致，跳過排標題列是正確行為。**不需修改**。

---

## ✅ Bug 8：`server/services/depthWriter.js`（Medium）

### 問題
`writeColoredDepthPallets` 寫彩色格式時，批號行（row+1）的 `bgColorRequest` 漏掉了——只對 SKU 行設背景色，批號行背景色保持白色（#ffffff），導致棧板下半部沒有色塊。

### 修法
在批號行（`nextRow+1`）新增 `bgColorRequest(sheetId, nextRow+1, prodCol, 2, 2, bg)`，對齊 GAS `writeColoredDepthPallets_:707-708` 的雙行背景色設定。

---

## ⏭️ Bug 9：`server/routes/undo.js`（Skipped）

### 問題
`undoStack` / `redoStack` 為模組級變數，所有使用者共用。A 操作者的 undo 可能 undo 掉 B 操作者的移動。

### 決策
**暫不修改**，原因：
1. `moveService.js` 的 `async-mutex` 已序列化所有寫操作，不會造成資料損壞。
2. GAS 版使用 `PropertiesService.getUserProperties()` 是 per-user 的，但 Node.js 版要實作 per-user undo 需要 session 管理，目前架構不支援。
3. 目前為單人開發/測試階段，此問題優先度低。
4. 記錄於此，待系統多使用者時再處理。

---

## ✅ Bug 10：`server/services/zoneViewService.js`（Medium）

### 問題
1. **Header filter 太激進**：`!v.includes('數') && !v.includes('量')` 會過濾掉任何含「數」「量」字的 slotId（例如「12號數量」之類的合法 ID）。
2. **`toGasRow` 缺少 `BoxQtyFontColor`、`PieceQtyFontColor`、`IsLastPallet`、`Status`、`Remarks`**，前端 `qtyStatusHtml()` 和顏色顯示依賴這些欄位。

### 修法
- Header filter 改為精確比對 `v !== '數量'`（與 GAS `parseWarehouseGrid:156` 的 `slotVal !== '數量'` 一致）。
- `toGasRow` 補齊所有欄位：
  - `BoxQtyFontColor`：取 pallet 的 `p.BoxQtyFontColor`
  - `PieceQtyFontColor`：取 pallet 的 `p.PieceQtyFontColor`
  - `IsLastPallet`：`bgColor !== 'GREEN' && fontColor === 'BLUE'`
  - `Status`：`bgColor === 'GREEN' ? '專案庫存' : '混板/散板'`（修正原本的 `'正常庫存'`）
  - `Remarks: ''`

---

## ✅ Bug 11：`server/routes/dispatch.js`（Medium）

### 問題
`movePallet` handler 從 palletKey 解析出 batch 後沒有傳給 `movePallet()`，導致 Bug 6 的 batch match 無法生效。

### 修法
從 palletKey 解析 `batch = parts[5] || ''`，並傳入 `movePallet(sheet, fromSlot, fromDepth, destDepth, sku, batch)`。

---

## ✅ Bug 12：`server/sheets/reader.js`（Medium）

### 問題
Sheets API 的 `values.get` 回傳的 `values` 可能因尾端空列被截短（比 `sheets.get` 的 `rowData` 短），但 `values.map()` 以 `values.length` 為準，導致 `rowData[ri]` 越界（undefined）→ `fmtRow` 為空 → 顏色/粗體資訊遺失。

### 修法
改用 `Math.max(values.length, rowData.length)` 作為列數，以 `values[ri] || []` 取代 `values[ri]`，確保越界時回傳空陣列而非 crash。

---

## 📊 修復統計

| 嚴重度 | 數量 | 檔案 |
|--------|------|------|
| Critical | 5（Bug 1–5） | colorUtils.js, depthSection.js |
| High | 1（Bug 6） | moveService.js |
| Medium | 5（Bug 8, 10–12 + Bug 9 記錄） | depthWriter.js, zoneViewService.js, dispatch.js, reader.js |
| Verified OK | 1（Bug 7） | gridUtils.js（不需修改） |

### 修改的檔案清單

| 檔案 | 變更摘要 |
|------|---------|
| `server/utils/colorUtils.js` | 完全重寫：RGB ratio 演算法對齊 GAS，新增 parseHex、FontColor BLUE/RED/GREEN |
| `server/services/depthSection.js` | 預掃 sectionHasBold、回傳 BoxQtyFontColor/PieceQtyFontColor |
| `server/services/moveService.js` | `movePallet` 新增 batch 參數，SKU+batch 精確匹配 |
| `server/services/depthWriter.js` | 批號行補上 bgColorRequest |
| `server/services/zoneViewService.js` | header filter 改精確比對、toGasRow 補齊所有欄位 |
| `server/routes/dispatch.js` | movePallet handler 傳入 batch |
| `server/sheets/reader.js` | 修正 values/rowData 長度不一致導致越界 |

---

## 🔜 下一步建議

1. **本機測試**：`npm start` → 開 `http://localhost:3000`，對假庫位表做完整流程測試（讀取 → 搬移 → 還原 → 重做）。
2. **重點驗證**：
   - 粉紅色棧板（`#ff6666`、`#e06666`）是否被正確辨識為 RED
   - 數量欄的「已回報/待重點」狀態是否正確顯示（FontColor BLUE → 最後一板）
   - 彩色格式搬移後批號行背景色是否正常
   - 白色格式 + 彩色格式混合排段的搬移
3. **前端 Regression Test**：確認 `index.html` 的 `qtyStatusHtml()` 與 `IsLastPallet` 顯示正常。
4. **部署**：確認無误後 push 到 main 觸發 cPanel 自動部署。
